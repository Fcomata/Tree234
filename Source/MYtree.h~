#include "tree_node.h"
#include <string>
#include <sstream>
#include <algorithm>
#include <fstream>

template<typename T>

class tree{


    public:

	node* root;
	node* temp;

        tree<int>(T);
        ~tree();
	string cadena;

	node* insert(T key);
        bool search(T key);
	void prT();
	
	
	


	
        


    private:

	bool insert(T key, node*);
        bool search(T key, node *leaf);
	void print_search(node* leaf);
	node* devolver();
	void print_search();
	string to_string(T);
	void print_node(node* leaf);
	void coma();
	void flecha();
	void flecha2();
	void fin();
	
        
};
